package com.webcheckers.Checkers;

import com.webcheckers.model.Space;

import java.util.Iterator;

public class RowIterator implements Iterator<Space> {

    // The space currently being pointed to
    private Space current;

    public RowIterator() { }

    // Stubbed out for now
    @Override
    public boolean hasNext() {
        return true;
    }

    // Stubbed out for now
    @Override
    public Space next() {
        return null;
    }

}
